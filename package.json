{
  "name": "now",
  "version": "1.0.0",
  "description": "Quick start project.",
  "scripts": {
    "dev": "npx zx ./scripts/build.mjs --env development && npx serve -p 8866",
    "build": "npx zx ./scripts/build.mjs --env production",
    "gen:profiles": "npx zx ./scripts/profiles.mjs",
    "prepare": "husky install"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/boxtoys/now.git"
  },
  "bugs": {
    "url": "https://github.com/boxtoys/now/issues"
  },
  "homepage": "https://github.com/boxtoys/now#readme",
  "dependencies": {
    "zx": "^7.2.3"
  },
  "devDependencies": {
    "dotenv": "^16.3.1",
    "serve": "^14.2.1",
    "husky": "^8.0.0"
  }
}
